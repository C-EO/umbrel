version: '3.7'

services:
  server:
    image: getumbrel/community-app-store-hello-world:latest
    restart: on-failure
    user: '1000:1000'
    init: true
    volumes:
      - ${APP_DATA_DIR}/data/server:/data
