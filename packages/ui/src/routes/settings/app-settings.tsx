/**
 * Settings → App settings. The globally mounted app settings dialog presents
 * its app-list step whenever this pathname is active (and continues into a
 * chosen app's settings within the same dialog), so this route only claims
 * the URL and keeps the settings page behind the dialog.
 */
export default function AppSettingsListRoute() {
	return null
}
